import { ThunkAction, ThunkDispatch } from "redux-thunk";
import { AnyAction } from "redux";
import { IStoreState } from "src/redux/reducers";
export const API_KEY = "";
export const SERVER_BASE_URL = "";
export const SERVER_IMAGE_BASE_URL = "";

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// #region TS INTERFACES & TYPES
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
export type ThunkDispatchAction = ThunkDispatch<IStoreState, null, AnyAction>;
export type ThunkResult<R> = ThunkAction<R, IStoreState, null, AnyAction>;

// #endregion TS INTERFACES & TYPES
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
